# 스마트 팩토리 자동화 프로젝트 요구사항 명세서

## 1. 목적

이 프로젝트는 Python 프로그램으로 Factory I/O 가상 공장을 제어하는 것을 목표로 한다. 공장에 들어온 파란색과 초록색 물체를 확인하고, 각각 정해진 가공기와 분류 위치로 이동시키도록 만든다.

## 2. 통신 방식

- Python 프로그램은 Modbus TCP 통신을 이용해 Factory I/O와 연결한다.
- 연결 설정은 IP `210.119.14.76`, Port `502`, Unit ID `1`을 사용한다.
- 프로그램은 센서 상태를 계속 확인하고, 필요한 경우 컨베이어·가공기·푸셔에 동작 신호를 보낸다.
- 통신이 잠시 끊기거나 늦어져도 프로그램이 바로 종료되지 않도록 재연결 기능과 오류 기록 기능을 넣는다.
- 실행 전에는 Factory I/O의 장치 주소와 프로그램에서 사용하는 주소가 같은지 확인한다.

## 3. 주요 기능

### 3.1 가공기 제어

- 파란색 물체는 `MC0`, 초록색 물체는 `MC1` 가공기로 보낸다.
- 프로그램은 가공기가 쉬는 중인지, 작업 중인지, 작업이 끝났는지를 계속 확인한다.
- 가공기에서 오류가 발생하면 초기화 또는 복구 동작을 시도한다.
- 작업이 너무 오래 걸릴 경우 무한히 기다리지 않도록 제한 시간을 둔다.

### 3.2 물체 이동 및 분류

- 물체를 생산하는 작업과 물체를 분류하는 작업은 동시에 진행한다.
- 물체가 한꺼번에 몰리지 않도록 최대 4개까지만 순서를 기다리게 한다.
- 대기 공간이 가득 차면 물체를 무리하게 처리하지 않고, 해당 상황을 기록한다.
- 센서가 푸셔 앞에 도착한 물체를 감지하면, 제어 프로그램이 물체 정보를 확인한다.
- 파란색 물체로 판단되면 프로그램이 푸셔에 신호를 보내 물체를 옆으로 민다.
- 컨베이어, 푸셔, 물체 배출 장치는 정해진 주소에 맞춰 제어한다.

### 3.3 안전한 동시 작업

- 여러 작업이 동시에 Modbus 통신을 사용하더라도 신호가 섞이지 않도록 한 번에 한 작업만 통신하도록 관리한다.
- 물체 순서와 현재 상태를 여러 작업이 함께 사용할 때 값이 꼬이지 않도록 보호한다.
- 이를 위해 Python의 `threading.Lock` 기능을 사용한다.

## 4. 오류 확인 및 점검 기능

- 센서 상태와 대기 중인 물체 수를 화면 또는 로그로 확인할 수 있어야 한다.
- 통신 오류, 센서 미감지, 작업 시간 초과, 대기열 초과 상황을 기록한다.
- 푸셔만 따로 움직여 보는 시험 기능과 `MC0` 가공기 시험 기능을 제공한다.
- 오류가 발생해도 프로그램 전체가 바로 멈추지 않도록 안전하게 처리하고, 가능한 경우 다시 연결하거나 복구한다.

## 5. 확인 방법

- 파란색 물체가 `MC0` 가공기로 이동하는지 확인한다.
- 초록색 물체가 `MC1` 가공기로 이동하는지 확인한다.
- 파란색 물체가 푸셔 위치에 도착했을 때, 센서 감지와 프로그램 판단 후 정상적으로 밀려 분류되는지 확인한다.
- 생산 작업과 분류 작업이 동시에 진행되는지 확인한다.
- 대기 중인 물체 수가 4개를 넘지 않는지 확인한다.
- 통신이 끊긴 뒤 다시 연결을 시도하는지 확인한다.

