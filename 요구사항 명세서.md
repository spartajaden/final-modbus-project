# 스마트 팩토리 자동화 프로젝트 요구사항 명세서

## 1. 목적

이 프로젝트는 Python 프로그램으로 Factory I/O 가상 공장을 제어하는 것을 목표로 한다. 파란색과 초록색 물체를 감지하고, 각각의 가공·분류 흐름을 제어한다.

## 2. 통신 방식

- Python 프로그램은 Modbus TCP로 Factory I/O와 연결한다.
- 연결 설정은 IP `210.119.14.76`, Port `502`, Unit ID `1`을 사용한다.
- 프로그램은 센서와 가공기 상태를 폴링 방식으로 확인하고, 컨베이어·가공기·푸셔에 코일 명령을 보낸다.
- 프로그램 시작 시 Factory I/O 연결을 최대 5회 시도한다.
- 실행 전 Factory I/O의 I/O 주소와 `pyfa-project.py`의 I/O 매핑이 같은지 확인한다.

## 3. 주요 기능

### 3.1 가공기 제어

- Blue 물체는 `MC0`, Green 물체는 `MC1` 처리 흐름에 사용한다.
- 각 가공기의 Busy 상태와 진행 레지스터를 확인해 작업 시작과 완료를 판단한다.
- 가공기 Error 상태가 감지되면 해당 가공기의 Reset 코일을 동작시킨다.
- 작업 시작·완료 대기에 시간 제한을 두며, 시작이 확인되지 않으면 Start 신호를 최대 3회 재시도한다.

### 3.2 물체 이동 및 분류

- 생산 작업과 분류 작업은 각각 `production_loop`, `sorting_loop`에서 동시에 수행한다.
- 비전 센서가 Blue 값을 감지하면 `blue_push_pending` 예약 건수를 증가시키고, 푸셔 동작을 순차 처리한다.
- Green 값이 감지되면 푸셔를 끈 상태로 물체가 다음 공정으로 이동하도록 한다.
- 푸셔 동작에는 지연 시간과 전진·후진 대기 시간 제한을 적용한다.
- 현재 구현에는 최대 대기열 크기 제한이 없다.

### 3.3 동시 작업 관리

- `blue_push_lock`으로 푸셔 예약 건수와 푸셔 동작 상태를 보호한다.
- `machining_lock`으로 동시에 진행 중인 가공 작업 수를 보호한다.
- `modbus_record_lock`으로 CSV 기록 파일에 동시에 쓰는 상황을 보호한다.
- 모든 Modbus 읽기·쓰기 호출을 하나의 Lock으로 직렬화하지는 않는다.

## 4. 오류 확인 및 기록

- `modbus_record_loop`는 Modbus 상태 변화와 일부 오류를 CSV 파일에 기록한다.
- 가공 시작·완료, 푸셔 전진·후진 과정에 시간 제한을 적용한다.
- Modbus 읽기·쓰기 결과가 오류이면 예외를 발생시킨다.
- `Ctrl+C` 또는 `stop_all()` 실행 시 제어 출력을 끄고 공정 상태를 정지한다.
- 운전 중 네트워크 단절에 대한 완전한 자동 재연결·자동 복구 기능은 현재 구현 범위에 포함하지 않는다.

## 5. 확인 방법

- Factory I/O가 RUN 상태이고 Driver가 **Modbus TCP/IP Server**인지 확인한다.
- Python 프로그램이 Factory I/O에 연결되는지 확인한다.
- Blue 물체가 `MC0` 처리 흐름과 푸셔 분류 과정을 거치는지 확인한다.
- Green 물체가 `MC1` 처리 흐름을 거치고 푸셔 없이 이동하는지 확인한다.
- 생산 작업과 분류 작업이 동시에 실행되는지 확인한다.
- Blue 물체가 연속 감지될 때 푸셔 예약 건수가 순차적으로 처리되는지 확인한다.
- `Ctrl+C` 실행 후 컨베이어·가공기·푸셔 출력이 정지하는지 확인한다.