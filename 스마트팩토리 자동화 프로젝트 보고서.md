# 스마트 팩토리 자동화 프로젝트 보고서

## 1. 서론

### 연구 배경 및 목적

본 프로젝트는 Factory I/O 가상 공장을 Python과 Modbus TCP로 제어하는 자동화 실습이다. Python 프로그램이 센서 입력을 읽고 컨베이어, 가공기, 푸셔에 제어 신호를 보내 파란색과 초록색 물체의 가공·분류 과정을 수행하도록 구현했다.

Factory I/O 환경에서 가공기(MC0, MC1), 컨베이어, 푸셔, 비전 센서의 상태를 Modbus TCP로 교환하고, Python 기반 제어가 가능한지 확인하는 것을 목표로 했다.

### 구현 확인 항목

- 생산 작업과 분류 작업을 별도 스레드로 실행해 역할을 분리했다.
- 비전 센서가 Blue 값을 감지하면 푸셔 동작을 예약하고, Green 값이면 푸셔를 동작시키지 않도록 구현했다.
- 공유 상태와 로그 파일 접근을 Lock으로 보호하고, 작업·푸셔 대기 시간 제한을 적용했다.

## 2. 시스템 구성 및 통신 아키텍처

### 네트워크 및 통신 환경

- 통신 규격: Modbus TCP
- 연결 설정: IP `210.119.14.76`, Port `502`, Unit ID `1`
- Factory I/O Driver: Modbus TCP/IP Server

프로그램 시작 시 Factory I/O 연결을 최대 5회 시도한다. 연결에 실패하면 오류를 알리고 실행을 중단하므로, 운전 중 네트워크 단절에 대한 완전한 자동 복구 기능을 구현한 것은 아니다.

### 시스템 데이터 흐름

Python 제어 프로그램은 폴링 방식으로 이산 입력과 입력 레지스터를 읽는다. 읽은 센서·가공기 상태를 바탕으로 코일 쓰기 명령을 보내 컨베이어, 가공기, 에미터, 푸셔를 제어한다.

```text
Python 제어 프로그램 ↔ Modbus TCP ↔ Factory I/O 가상 공장
센서·가공기 상태 읽기               코일 명령으로 장치 제어
```

### I/O 매핑 설계

- 입력: 가공기 Busy/Error 상태, 물체 감지 센서, 비전 센서 값, 푸셔 전·후진 위치
- 출력: 에미터, MC0·MC1의 Start/Stop/Reset/Produce Lids, 푸셔, 컨베이어

## 3. 핵심 제어 알고리즘 및 구현

### 생산 및 가공 공정 제어

`MC0`은 Blue 자재, `MC1`은 Green 자재 처리 흐름에 사용한다. 두 가공 작업은 별도 스레드로 시작하며, 각 작업은 Busy 상태와 진행 레지스터를 폴링해 시작·완료를 확인한다.

가공 시작과 완료에는 시간 제한을 적용한다. 시작이 확인되지 않으면 Start 신호를 최대 3회 다시 시도하고, Error 입력이 감지되면 해당 가공기의 Reset 코일을 동작시킨다. 이는 개별 가공 작업의 제한된 재시도·초기화 처리이며, 전체 공정의 자동 복구를 의미하지는 않는다.

### 스레드 기반 생산·분류 제어

- `production_loop`: MC0와 MC1의 가공 작업을 반복 실행한다.
- `sorting_loop`: 비전 센서 값을 지속적으로 읽어 Blue와 Green을 구분한다.
- Blue 감지: `blue_push_pending` 예약 건수를 증가시키고, 푸셔 동작을 순차적으로 처리한다.
- Green 감지: 푸셔를 끈 상태로 물체가 다음 공정으로 이동하도록 한다.

`blue_push_pending`은 Blue 제품의 푸셔 예약 건수를 관리하는 변수다. 현재 구현에는 `MAX_QUEUE_SIZE = 4`와 같은 최대 대기열 크기 제한이 없다.

푸셔는 Blue 감지 후 설정된 지연 시간과 전진·후진 대기 시간 제한을 사용해 동작한다. `sort_done_event`와 `exit_event`는 분류 완료와 출구 도착 상태를 기록하는 이벤트 객체로 사용한다.

### 동기화 및 Lock 사용

- `blue_push_lock`: 푸셔 예약 건수와 푸셔 동작 상태를 순차적으로 관리한다.
- `machining_lock`: 동시에 진행 중인 가공 작업 수를 보호한다.
- `modbus_record_lock`: CSV 형식의 Modbus 기록 파일에 동시에 쓰는 상황을 보호한다.

현재 Lock은 공유 상태와 로그 파일 접근을 보호한다. 모든 Modbus 읽기·쓰기 호출을 하나의 Lock으로 직렬화하는 구조는 아니다.

## 4. 시스템 진단 및 예외 처리

- 상태 기록: `modbus_record_loop`가 Modbus 상태 변화와 일부 오류를 CSV 기록 파일에 남긴다.
- 시간 제한: 가공 시작·완료 및 푸셔 전진·후진 대기에 제한 시간을 적용한다.
- 안전 정지: `Ctrl+C` 또는 `stop_all()` 실행 시 제어 출력을 끄고 로컬 공정 상태를 정지한다.
- 오류 처리: Modbus 읽기·쓰기 결과가 오류이면 예외를 발생시키며, 가공기 Error 상태에서는 Reset을 시도한다.

## 5. 결론 및 향후 개선 방안

### 구현 결과

- Factory I/O 가상 환경에서 Python과 Modbus TCP를 이용한 가공·분류 제어 흐름을 구현했다.
- 생산 스레드와 분류 스레드를 분리하고, Blue 제품의 푸셔 예약 건수를 관리해 순차적인 푸셔 동작을 구성했다.
- 센서 상태 폴링, 가공·푸셔 대기 시간 제한, 출력 정지, CSV 기록 기능을 적용했다.

### 한계점 및 향후 과제

- 실제 PLC·DCS 등 현장 장비와의 연동은 검증하지 않았으며, 현재 결과는 Factory I/O 가상 환경 기준이다.
- 운전 중 통신 단절과 센서 오작동을 자동으로 복구하는 기능은 완전하지 않다. 재연결, 오류 상태 복구, 폴백 절차를 추가로 설계·검증할 필요가 있다.
- 푸셔 예약 건수에 상한과 과부하 처리 정책을 추가하고, 실제 측정 데이터를 바탕으로 처리 시간과 안정성을 평가할 필요가 있다.
